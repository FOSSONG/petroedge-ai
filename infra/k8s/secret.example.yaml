apiVersion: v1
kind: Secret
metadata:
  name: petroedge-secret
  namespace: petroedge
type: Opaque
stringData:
  JWT_SECRET: "replace-in-production"
  DATABASE_URL: "postgresql+psycopg://petroedge:petroedge@postgres:5432/petroedge"

